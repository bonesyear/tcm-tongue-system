# K3 复核报告（tcm-tongue-system，2026-09-17）

> 复核者：Kimi Code CLI，模型 **kimi-k3**（新会话 session_83cc81a8，非 resume——旧会话绑定 K2.7）
> 复核对象：`docs/CODE_REVIEW_2026-08-25_KIMI.md`（18 条问题）+ `docs/REPAIR_PLAN_2026-08-25.md`（3 轮次规划 + 方案 A 11 项）
> 背景：2026-08-25 的审查/规划全部由 K2.7 完成（当时 config 默认 K2.7），K2.7 有错判前科 → 判断层须 K3 复核。
> 模式：只读复核（未改任何文件、未执行 git 操作）。
> **总评：需修订后执行**（修订点见本文 ①②③ 与 REPAIR_PLAN 的"K3 复核修订"章节）。

---

# 复核结论

## ① 18 条问题清单：总体准确，但有 1 条确凿误判、2 条前提/范围有误

**确凿误判：**

- **#15 ❌ 误判**。`docs/architecture/dimension-overlap-analysis.md` 实际存在，且其 §5.2「证据覆盖度计算」正是 LOW/MEDIUM/HIGH 置信度映射表，与 `src/confidence.py:9` 的引用完全吻合。该引用准确无误，不需要修复，轮次 3 的 #15 应整条删除。

**前提或范围有误：**

- **#11 ❌ 前提不成立**。清单称"改成累加封顶现有测试不会红"——错误。`tests/test_scoring.py:176-194`（`test_score_with_structured_dict_input`：白厚 7 + 胖大 7 → 断言均值 7.0）和 `tests/test_weekly_report.py:49-50`（fixture 齿痕 4 + 胖大 7 → 断言 "5.5"）都是"多异常取平均"断言；改成累加封顶（14→10）两者必红。补一条显式断言无害，但此条应从"缺测试"降级为"可选冗余"。
- **#9 ⚠️ 范围多算一半**。只有 `get_pattern_differentiation`（`src/record.py:263`，`diag.get("许家栋经方辨证", diag)`）会把整个 diag 当 fallback；`get_formula`（`src/record.py:277`）默认就是 `diag.get("方剂建议", {})`，返回 `{}`，无此问题。修复只涉及一个方法。

**已被文档自身增量确认推翻的：**

- **#4 ⚠️ 原判不成立**。`startswith("DASHSCOPE_API_KEY=")` 本就免疫行首 `#` 注释，增量确认第 3 点已实测推翻，真实风险是行内注释污染（`KEY=xxx # 备注`）。当前代码 `scripts/vision_client.py:22`（跳过注释行）和 `:28`（剥离行内注释）已双重修复，"已消"结论属实。但审查报告顶部的"总体结论"仍把 #4 列为最值得修的 2 个问题之一，属文档内部自相矛盾的残留。

**逐条核实成立（与当前代码比对，行号已因 load_key 重构漂移）：**

| # | 结论 | 依据 |
|---|---|---|
| 1 | ✅ | `vision_client.py:53` `open()` 未用 with |
| 2 | ✅ 最严重 | `:65-67` 无 HTTPError/URLError 捕获，`:67` 裸取 `data["choices"]` |
| 3 | ✅ | `:71-72,77` 裸取 argv；另可顺带补：未知 mode 静默无输出（`:73-79` 无 else 分支） |
| 5 | ✅ | `scoring.py:205` `非(?!常)`；注意移除"非"后 `test_scoring.py:65` 注释（"未/没/非"）与 `:81-84`「非常」测试需同步梳理解释 |
| 6 | ✅ | `:57` 写死 `image/jpeg` |
| 7 | ✅ | `:75` 直接输出未校验 |
| 8 | ✅ | `record.py:146-148` 9 键白名单；修复时可直接复用现成的 `_flatten_to_text`（`:151`）做兜底扫描 |
| 10 | ✅ | `confidence.py:97-100`，`HIGH1`/`HIGH_2` 确实被解析为 HIGH |
| 12 | ✅ | `test_record.py:64-68` 仅覆盖形状 B 空列表 |
| 13 | ✅ | `scoring.py:188-197` 8 项 vs `DIMENSION_RULES` 9 项，事实成立（已被产品决策收口） |
| 14 | ✅ | tests/ 下确无 vision 测试 |
| 16 | ✅ 信息级 | `record.py:23` / `dimensions.py:13`，`/codebase-design` 是 skill 名非仓库文件 |
| 17 | ✅ 事实正确 | `confidence.py:123-125`，跳过合理 |
| 18 | ✅ 已修 | docstring `vision_client.py:6` 已是 `<part-key>` |

**功能忠实度三条结论全部复核无误**：`input_validator.py:208-216` 与 `generate_weekly_report.py:575-583` 确为"自报与推断取严格者"；`confidence.py:32-43` 与 `adaptive_analysis_prompt.md:118` §3.1 一致；`record.py:308-325` 问诊计数不进置信度。

**遗漏检查：无重大遗漏**。两条小补充：① `vision_client.py:52` 用 `assert` 做运行时校验，`python -O` 下失效且错误体验差，可并入 #2/#3 修复；② `record.py:295` `flag.get("triggered") is True` 严格布尔——LLM 若输出字符串 `"true"` 会静默不触发（fail-open），在安全红线语义下值得一句注释或宽松化，实际风险低。

## ② 三轮次设计：合理，需 3 处修正

按文件域聚合 + 域内按严重度 + 每轮全量测试/独立 commit + 中断风险分析，结构本身是好的。修正点：

1. **轮次 3 删除 #15 后只剩 #16**，单条文档注释不值得独立一轮，建议并入轮次 2 末尾顺带做掉。
2. **方案 A 落地清单第 1 项改的是 `vision_client.py`（PROMPTS["舌面"]），却排在轮次 2**——跨轮次文件归属。这导致轮次 1 结束时 vision_client.py 并非终态，且轮次 1 的 #14 测试可能与 PROMPTS 变更相互牵扯。建议：把方案 A 第 1 项挪入轮次 1，或在轮次 2 显式标注"再次触碰 vision_client.py"。
3. **#14 测试文件名残留旧名**：两份文档都写 `tests/test_vision_qwen.py`，文件已改名，应为 `tests/test_vision_client.py`。

一个可辩护但需明知的取舍：轮次 1 整体先于辨证层，意味着 P0 的 #5/#8/#9 排在 vision 的 P1/P2（#1/#6/#7/#14）之后。若严格"严重度优先"应先修 #5/#8/#9。考虑到每轮独立 commit 且同日执行，文件域分组可以接受，规划中的中断风险分析也已覆盖此点。

依赖图 `#11 ← #5` 随 #11 降级可删；`#14 ← #2/#3/#6/#7` 保持。

## ③ 方案 A 11 项清单：方向正确，4 处漏洞/多余

"评分层移除、辨证层保留"的语义划分清晰，双计结论复核成立（结构化输入两字段独立；纯文本重复计分确为 `scoring.py:313-315` 已注释的既有局限）。问题在细节：

1. **项 10 前提不成立，应先验证再动手**。fixture `tests/fixtures/2026-06-25_analysis.json:12` 的 `luster` 是"荣润"= **0 分**，且 `score()` 的分母只计 v>0 的指标（`scoring.py:323`），移除 body_luster 后该 fixture 舌诊均值不变——`test_weekly_report.py:50` 的 "5.5" 和 `:38-41` 的八键集合断言都不受影响。"历史评分口径会变"只适用于含"少泽/枯槁"的记录，对这个 fixture 是空话。预计无需改 fixture，跑一遍全量测试即可证实。
2. **项 4 保留 `TONGUE_BODY_LUSTER_MAP` 即成死代码**。从 `DIMENSION_RULES` 移除后无任何消费者（已全库 grep 确认）；且其词汇表是荣枯体系（荣润/少泽/枯槁），与新润燥词汇（润泽/偏干/干燥/干裂）不匹配，"历史兼容"名不副实。建议直接删除（符合 ponytail 原则）；若坚持保留，清单漏了 `scoring.py:111` 头注释 `# 舌质荣枯（有神/无神）` 的同步修改。
3. **项 2 缺历史兼容说明**。A 路径改名后，存量形状 A 记录的"舌质荣枯"会在 `get_observation` 中静默丢失。实测 `records/daily` 60 个文件全部用英文 `luster` 键（形状 B），无实际影响——但清单应注明"已确认无存量形状 A 数据"，或给路径加 fallback（先试 `["舌质润燥"]` 再试 `["舌质荣枯"]`）。**项 2 与项 11 必须同 commit**：validator 经 `SCHEMA.dimension_indicators` 数据驱动自 `_DIMENSION_INDICATORS`（`record.py:104-108`），模板键与 A 路径不同步会造成校验漂移，清单未标注此耦合。
4. **项 1 措辞回退**。前一段决策明确要求"明确不判神气"，11 项版只写了"粗判"四级分级，应保留"不判神气/荣枯"的否定指令。另建议（非必须）：新 A 键"舌质润燥"与既有"舌苔润燥"（`record.py:54`）仅一字之差，LLM 易混，可考虑"舌质润燥度"。

项 9 措辞收窄即可：B 路径不变，只需补形状 A "舌质润燥"解析测试（B 侧加一条回归即可）。

**总评：需修订后执行**——清单层 #15 确凿误判（引用文件存在且 §5.2 内容吻合）、#11 前提不成立、#9 范围多算一个方法、#4 已被自身增量确认推翻但"总体结论"残留；轮次设计主体合理，需删 #15、降级 #11、归位方案 A 第 1 项并修正测试文件名；方案 A 方向正确，需补死代码处置（删 MAP）、项 10 改"先验证"、标注项 2+11 同 commit 耦合。修订以上各点后可按轮次 1→2→3 执行。
