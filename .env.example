# 望诊识图配置 —— 复制为 .env 或导出为环境变量
# 支持任意 OpenAI 兼容多模态模型；各参数取值以你所用厂商的官方文档为准
# ⚠️ 代码内置默认值（qwen3.8-max + DashScope 端点）仅为示例——目的是 clone 后
#    不配置也能看到完整结构；请务必替换为你自己的模型与端点。

# 必填三件套
VISION_MODEL=your-model-name
VISION_BASE_URL=https://your-endpoint.example.com/v1/chat/completions
VISION_API_KEY=<your-api-key-here>

# 可选参数（不设置则不发送/使用脚本默认值）
# VISION_TEMPERATURE=
#   不设置 = payload 不含 temperature 键，由模型默认值生效。
#   ⚠️ 设 0 不等于「确定性输出」：部分模型有官方下限（如 qwen3.8-max 视觉理解
#   下限 0.6，更低值被服务端静默改写），请查官方文档的 default/range/clamp 三栏。
# VISION_MAX_TOKENS=600
#   默认 600。思考型（reasoning）模型的思考过程会占用该额度，建议 ≥8000。
# VISION_TIMEOUT=150
#   observe 单次请求超时秒数，默认 150（classify 固定 60）。

# 换模型后必做：见 README「配置自己的视觉模型」checklist
